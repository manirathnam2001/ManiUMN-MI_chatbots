# Email Configuration (Recommended: Use environment variables for security)
# Copy this file to .env and fill in your actual credentials

# SMTP Server Settings
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USE_SSL=true

# SMTP Credentials (Required for sending emails)
# For Gmail: Use App Password (https://myaccount.google.com/apppasswords)
SMTP_USERNAME=your-email@umn.edu
SMTP_APP_PASSWORD=your-app-password-here

# Alternative: You can also configure these in config.json
# However, environment variables are more secure and take priority

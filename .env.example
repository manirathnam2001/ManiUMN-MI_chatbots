# ====================================================================
# MI Chatbots - Environment Variables Configuration
# ====================================================================
# Copy this file to .env and fill in your actual credentials
# NEVER commit the .env file to version control!
#
# For security:
# - Keep this file secret and secure
# - Use strong, unique passwords
# - Rotate credentials regularly
# - Never share credentials in plain text
# ====================================================================

# --------------------------------------------------------------------
# GROQ API Configuration (Required for chatbot functionality)
# --------------------------------------------------------------------
# Get your API key from: https://docs.newo.ai/docs/groq-api-keys
GROQ_API_KEY=your-groq-api-key-here

# --------------------------------------------------------------------
# SMTP Server Settings (Required for email functionality)
# --------------------------------------------------------------------
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USE_SSL=true

# --------------------------------------------------------------------
# SMTP Authentication (Required for email functionality)
# --------------------------------------------------------------------
# For Gmail: 
#   1. Enable 2-factor authentication on your Google account
#   2. Generate an App Password at: https://myaccount.google.com/apppasswords
#   3. Use the App Password (not your account password)
SMTP_USERNAME=your-email@umn.edu
SMTP_APP_PASSWORD=your-app-password-here

# --------------------------------------------------------------------
# Advanced Configuration (Optional)
# --------------------------------------------------------------------
# Connection timeout in seconds (default: 30)
# CONNECTION_TIMEOUT=30

# Number of retry attempts for failed operations (default: 3)
# RETRY_ATTEMPTS=3

# Delay between retries in seconds (default: 5)
# RETRY_DELAY=5

# Log level (default: INFO)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

